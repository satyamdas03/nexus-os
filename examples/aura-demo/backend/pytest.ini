[pytest]
markers =
    slow: tests that are slow or environment-sensitive
testpaths = tests
norecursedirs = .* venv node_modules generated_tests __pycache__
filterwarnings =
    ignore::DeprecationWarning:fastapi.*
    ignore::DeprecationWarning:starlette.*
